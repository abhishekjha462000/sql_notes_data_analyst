-- In order to import data from a csv file, we need to write click on the
-- "Tables" option in the left side pane, and then use the Table Import Data 
-- Wizard. However, this is a slow process and it might take some time depending
-- on the size of the data.

-- We shall be importing the smartphones dataset that we scraped using selenium.

-- The DML commands means data manipulation language commands. These include
-- CRUD operations i.e, CREATE, READ, UPDATE and DELETE. In real life, we do not
-- need to insert, update and delete the data manually as the data comes prepared.


